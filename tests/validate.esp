(**
 * @source validate.esp
 * @brief validated program
 * @use esper -p -e ./tests/validate.esp -o ./tests/validate.cc
 *)

let main: int = |argc: int, argv: vector<string>|
  esper.print("argc:", argc);

  for arg in argv
    esper.print("\n", arg)
  end;

  0
end
